txn = read.transactions(file='Transactions_sample.csv', "Loads the file"
                      rm.duplicates = FALSE,
                      format = "single",sep=",",cols = c(1,2));"creates coloumns to seperate"
